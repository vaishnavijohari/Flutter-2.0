import 'package:flutter/material.dart';

class CryptoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Stories'),
      ),
      body: Center(
        child: Text('This is the Crypto Screen'),
      ),
    );
  }
}
